<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="CustomEditText" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="android:textColor">@color/quaternary</item>
        <item name="android:textColorHint">@color/quaternary</item>
        <item name="android:textSize">16sp</item>
        <item name="boxStrokeColor">@color/tertiary</item>
        <item name="boxStrokeWidth">2dp</item>
    </style>

    <style name="CustomSpinner" parent="@android:style/Widget.Holo.DropDownItem.Spinner">
        <item name="android:textSize">16sp</item>
        <item name="android:background">@color/primary</item>
        <item name="android:popupBackground">@color/primary</item>
        <item name="android:textColor">@color/quaternary</item>
    </style>
</resources>